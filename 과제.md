# Track A-2: AutoEncoder·RUL — 실습 과제

> 실습 노트북을 실행한 후, 동일한 노트북 안에 새 셀을 추가하여 과제를 완성하세요.
> **새 라이브러리 설치 불필요 | 과제당 소요 시간: 약 15~20분**

---

## 📒 노트북 01 — AutoEncoder 이상탐지

**과제 A (필수)**
실습에서 `epochs=50`으로 학습했습니다.
`epochs=30`으로 줄여 재학습하고, 마지막 epoch의 val_loss를 출력하세요.

**과제 B (필수)**
재구성 오차 임계값을 **99 퍼센타일**로 바꾸고, 이상 탐지 수를 출력하세요.

```python
threshold_99 = np.percentile(train_losses, 99)
anomalies_99 = (test_losses > threshold_99).sum()
print(f"99 퍼센타일 임계값: {threshold_99:.4f} → 이상 {anomalies_99}개")
```

**과제 C (심화·선택)**
학습 loss와 val loss를 같은 그래프에 그려 과적합 여부를 확인하세요.

---

## 📒 노트북 02 — LSTM RUL 예측

**과제 A (필수)**
`window_size=30`을 `window_size=20`으로 바꿔 시퀀스를 재생성하고 RMSE를 출력하세요.

**과제 B (필수)**
테스트셋 처음 20개 샘플의 예측 RUL과 실제 RUL을 같은 그래프에 그리세요.

```python
plt.plot(y_test[:20], label='실제 RUL', marker='o')
plt.plot(y_pred[:20], label='예측 RUL', marker='x')
plt.legend()
plt.title('예측 vs 실제 RUL (처음 20개)')
plt.show()
```

**과제 C (심화·선택)**
엔진 ID별 RMSE를 계산하고 가장 잘 맞은 엔진 ID를 출력하세요.

---

## 📒 노트북 03 — 예방정비 스케줄·ROI

**과제 A (필수)**
월 고장 빈도를 **현재 값의 2배**로 바꾸고 ROI가 어떻게 달라지는지 출력하세요.

**과제 B (필수)**
정비 스케줄 결과에서 가장 빠른 정비 예정 시점을 찾아 셀 아래에 출력하세요.

**과제 C (심화·선택)**
사후정비 vs 예지보전 연간 비용을 막대그래프로 비교하세요.

```python
pd.Series({'사후정비': current_cost, '예지보전': predictive_cost}).plot(kind='bar', color=['red','green'])
plt.title('연간 정비 비용 비교')
```

---

## 📌 제출 방법

- 기존 노트북에 과제 셀을 추가하고 전체 실행 후 `.ipynb` 파일 제출
- **제출 기준**: 과제 A·B 실행 완료 + 결과 확인 + 관찰 주석 1줄
